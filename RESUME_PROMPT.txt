I'm continuing work on "MedCite" — a medical Q&A web app for the Jubilant Pharma hackathon.

STEP 1 — READ FIRST:
1. PITCH.md at the repo root — this is the canonical content source for the deck. Elevator pitch (§1), problem (§2), what we built (§3), the 7 hard rules (§4), architecture diagram (§5), the trust pitch verbatim (§6), the 5 hero queries with exact demo lines (§7), 2-min demo script (§8), 9 anticipated Q&A (§9), the numbers (§10), tech stack (§11), closing line (§13). DO NOT regenerate this — slide content is locked.
2. PROJECT_SPEC.md — non-negotiables in §3, API shape in §6, hero queries in §11, current state in §12.

ONLY REMAINING TASK: BUILD THE PITCH DECK FROM PITCH.md.

The system is fully shipped, deployed, verified, and the 2-minute backup demo video is recorded. Nothing left to code. Just build slides.

Suggested slide structure (10 slides, follows PITCH.md flow + 2-min demo script):
  1. Title  — "MedCite — clinical Q&A that never hallucinates"
  2. Problem  — PITCH.md §2
  3. What we built (with screenshot of the answered query)  — PITCH.md §3
  4. The trust pitch (4 sentences verbatim)  — PITCH.md §6
  5. The 7 hard rules  — PITCH.md §4
  6. Architecture diagram (the two-tier flow)  — PITCH.md §5
  7. Live demo placeholder slide (you'll switch to the browser here, hero queries 1-4)  — PITCH.md §7 + §8
  8. The numbers  — PITCH.md §10
  9. Q&A teaser (top 3 anticipated questions)  — PITCH.md §9
  10. Closing  — PITCH.md §13

Format choice: ask the user which they want — PowerPoint (.pptx via python-pptx), Google Slides (markdown they paste in), or marp/reveal.js markdown deck. Don't assume.

Screenshots needed for slides 3 + 7: prod URL is https://abhi04-medcite.vercel.app — open hero #2 (empagliflozin) for the "answered query" hero shot, and the abstention screen on hero #5 (acetaminophen) for the "honest abstention" demonstration.

CURRENT STATE OF THE DEPLOYED SYSTEM (no action needed; fully working):
- Backend: https://Tony0489-MedCite-api.hf.space (HF Spaces, Docker, free tier, 16 GB RAM, port 7860). LanceDB (53.5 MB, 17,456 chunks) bundled into the image. Self-improvement write-back fix shipped this session — "Added N articles to KB" chip is now actually true in real time.
- Frontend: https://abhi04-medcite.vercel.app (Vercel, Next.js 16, Tailwind 4). Health-check now retries 3× with 5s/10s/20s backoff and shows "Backend warming…" intermediate state during HF cold starts. PWA install layer shipped (manifest + icon + apple meta) — judges can install to home screen.
- Last commit on main: 96765df. Recent commits:
    96765df docs: log Day-3 PM polish round shipped
    bebebb1 feat(frontend): health-check retry with warming state + PWA install layer
    e2ca547 fix(backend): live-search PubMed retry without stopwords + share LanceDB connection so write-back surfaces
    2c1cf32 docs: lock Day-3 polish scope
    da24598 fix(demo): swap CAP-antibiotics live showstopper for H. pylori
- Repo: https://github.com/Abhishek0489/medCite.git on branch main. All work pushed.

PRE-DEMO STATE (no action needed, but understand the why):
Hero #3 was swapped from "H. pylori first-line eradication 2024" to "Levetiracetam status epilepticus dose" mid-session because the recording flow contaminated H. pylori (write-back wrote 26 articles, so it would Tier-1 hit instead of triggering the live-multi-AI showstopper). After the swap, the backend was redeployed via sync.ps1, which wiped both contaminations and restored the bundled image. Levetiracetam is verified fresh: PubMed 169 PMIDs, local cache top_sim=0.3896 (well below 0.55 threshold). Backup demo video shows H. pylori — that's fine, the live judge demo will show levetiracetam, both demonstrate the same live-multi-AI behavior.

If you somehow re-contaminate before the judge demo, reset (~90s):
  `.\deploy\hf\sync.ps1 -HfUser Tony0489 -HfSpace MedCite-api -HfToken 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`
Verify reset: POST /query/local with `{"query":"Levetiracetam status epilepticus dose"}` → expect status=not_found, top_sim≈0.39.

TWO MASTER QUERY SETS (full table in PITCH.md §14):

SET A — PRE-DEMO VERIFICATION (run ~30 min before judges; ~3 min total; designed to NOT contaminate Set B):
A1. Open https://abhi04-medcite.vercel.app → wait for green "backend healthy" indicator (HF cold start may take ~30s, frontend shows "Backend warming…" during retry)
A2. Type "Metformin contraindications in chronic kidney disease" → expect Tier-1 hit, conf ≥ 0.75, ~3 sec, 5 cited sources. Confirms synth+verify pipeline. NOT a hero query — won't taint demo
A3. Type "First-line therapy for ulcerative colitis" → NotFoundScreen → click Search live → ~20 sec → cited answer + "Added N articles" chip. Confirms Tier-2 PubMed pipeline + write-back. SACRIFICIAL — gastroenterology, never demoed, contamination is fine
A4. Type "Is acetaminophen safe in third trimester pregnancy?" → expect amber abstention screen, sim ≈ 0.45. Do NOT escalate. Confirms abstention threshold. READ-ONLY — abstention writes nothing to LanceDB so this is safe to ask before demoing the same query on stage
A5. CRITICAL contamination check via API (do NOT use the UI): `Invoke-RestMethod -Method POST -Uri https://Tony0489-MedCite-api.hf.space/query/local -ContentType 'application/json' -Body '{"query":"Levetiracetam status epilepticus dose"}'` → expect status=not_found, top_sim ≈ 0.39. Proves Set B #3 (the showstopper) is still uncontaminated. If top_sim > 0.55, run `.\deploy\hf\sync.ps1 ...` to reset (~90s) then re-verify A5

SET B — JUDGE DEMO (the 5 hero queries; chips on homepage pre-populated in this order):
1. "Does empagliflozin reduce cardiovascular mortality in HFpEF?" → Tier-1, conf 0.80, top_sim 0.82, RCT/Review badges
2. "First-line treatment for drug-resistant tuberculosis 2024" → Tier-1, top_sim ~0.80 (was 0.44 on Day 1 — self-improvement story)
3. "Levetiracetam status epilepticus dose" → live-multi-AI showstopper. Out of corpus (no neurology specialty). After live click: "Added 26 articles" chip + cited 3-sentence answer about loading doses. Re-asking shows Tier-1 hit (legitimate "system actually learned" follow-up if demo time allows)
4. "Is acetaminophen safe in third trimester pregnancy?" → abstention screen ("closest match scored 0.45"). Do NOT escalate — the abstention IS the feature
5. "Side effects of SGLT2 inhibitors in elderly patients" → Tier-1, Meta-analysis badge (backup if any of #1-4 misbehaves)

HARD RULES (from §3, do not violate):
1. LLMs never write URLs — backend stitches them from PMIDs in metadata
2. Synthesizer prompt forbids prior knowledge; chunks-only or INSUFFICIENT_EVIDENCE
3. Verifier must be a different vendor (Google synth + Meta/Llama verify)
4. Confidence < 0.75 → abstain
5. Live search only runs when user clicks button (never automatic)
6. Every source card shows quoted passage + clickable PubMed + DOI URL
7. UI is a clinical tool (one question, one answer card) — NOT a chatbot

THE 4-SENTENCE TRUST PITCH (PITCH.md §6, memorize verbatim):
"Every other AI tool starts with the LLM and asks it to remember sources. We start with PubMed and ask the LLM to summarize what's actually there. The LLM is never allowed to invent a citation, never allowed to answer from memory, and a second model from a different company verifies every claim before the doctor ever sees it. If it can't reach 75% confidence — it says so. That's the difference between a chatbot and a clinical tool."

ENV: Windows + PowerShell + Python 3.12 venv at backend/.venv/. Node 22 LTS. Backend can still be run locally for screenshots if needed (.venv\Scripts\Activate.ps1; uvicorn main:app --port 8000) but not required — the prod URL works for everything.

POWERSHELL GOTCHAS:
- `curl` is aliased to Invoke-WebRequest. Use `curl.exe` or `Invoke-RestMethod` for JSON
- `&&` is not a valid statement separator; use `;` instead
- Heredoc (`cat <<'EOF' ... EOF`) doesn't work; for long commit messages keep them short or write to .git/COMMIT_MSG_TMP first then `git commit -F`

WORKFLOW:
- Commit after each logical piece. Brief conventional commit messages (user prefers short).
- Never commit .env or frontend/.env.local (both gitignored).
- Don't pursue the optional HF Space keep-alive (UptimeRobot monitor) — user explicitly opted out.
- Don't pursue corpus expansion / Pro model swap / date-range widening (out of scope, would absorb the H. pylori showstopper into Tier 1).

SECURITY NOTE: HF write token `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` was pasted into chat — rotate via https://huggingface.co/settings/tokens once the hackathon is over.

Continue with: build the pitch deck from PITCH.md. Ask the user about format (PPTX / Google Slides markdown / marp / reveal.js) before generating slides.
